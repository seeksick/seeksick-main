# 웹 어플리케이션 추가 의존성

# Flask 및 웹 서버
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# OpenAI ChatGPT API
openai==1.12.0

# 환경변수 관리
python-dotenv==1.0.0

# 기존 requirements.txt 의존성도 필요합니다
# pip install -r requirements.txt
# pip install -r requirements-web.txt

